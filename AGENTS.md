# AGENTS.md — 이 저장소에서 활동하는 모든 AI 에이전트가 지켜야 할 규칙

이 파일은 Claude Code, Cursor, GitHub Copilot Workspace 등 이 저장소(`b-bot`)에서 코드를 작성/수정하는
모든 AI 에이전트에게 적용됩니다. 사람 사용자가 명시적으로 다른 지시를 내리지 않는 한 아래 규칙을 우선합니다.

---

## 1. Git 관련 — 가장 중요한 규칙

- **에이전트는 사람의 명시적 승인 없이 `git commit`을 실행하지 않는다.**
  - "커밋해줘", "커밋까지 해줘" 등 사람이 그 턴에 직접 요청한 경우에만 커밋을 실행한다.
  - 이전 대화에서 한 번 승인받았다고 해서 이후 턴에서도 자동으로 커밋해도 되는 것은 아니다. 매 커밋마다 다시 확인한다.
- **에이전트는 사람의 명시적 승인 없이 `git push`를 실행하지 않는다.** 원격 저장소에 반영되는 작업은 특히 신중해야 한다.
- **`--no-verify` 플래그를 절대 사용하지 않는다** (`git commit --no-verify`, `git push --no-verify` 등).
  pre-commit/pre-push 훅을 우회하는 행위는 이 프로젝트의 안전장치를 무력화하는 것과 같다.
- **훅 스크립트(`.husky/*`, `commitlint.config.js`, `.lintstagedrc.json`)를 임의로 수정하거나 완화하지 않는다.**
  훅이 실패하면:
  1. 실패 원인(lint 에러, 커밋 메시지 형식 오류, 테스트 실패 등)을 사람에게 명확히 보고한다.
  2. 원인이 되는 코드/메시지를 고치도록 제안하거나, 사람의 동의를 받아 직접 수정한다.
  3. 훅 자체를 비활성화하거나 우회하는 방식으로 "일단 통과"시키지 않는다.
- **강제 push(`git push --force`, `--force-with-lease`), 히스토리 재작성(`git rebase -i`, `git filter-branch` 등),
  브랜치 삭제**는 사람이 명시적으로 요청한 경우가 아니면 수행하지 않는다.
- **`main`/`develop` 등 보호 대상 브랜치에 직접 커밋하지 않는다.** 작업은 별도 브랜치(`feature/*`, `fix/*`)에서
  진행하고, 병합은 사람이 PR을 통해 진행하도록 안내한다 (에이전트가 직접 PR을 병합하지 않는다).
- 커밋 메시지는 `CONTRIBUTING.md`의 Conventional Commits 규칙을 따른다.

## 2. 커밋 전 항상 하는 절차

1. `git status`, `git diff`로 변경사항을 스스로 검토한다.
2. 변경사항 요약을 사람에게 보여준다 (무엇을 왜 바꿨는지).
3. 사람이 승인하면, `CONTRIBUTING.md` 규칙에 맞는 커밋 메시지를 제안한다.
4. 실제 커밋은 사람의 최종 승인(예: "그 메시지로 커밋해줘") 이후에만 실행한다.
5. `.husky/pre-commit`이 대화형 확인(`yes` 입력)을 요구하는 경우, 에이전트가 이를 대신 자동으로 입력하거나
   우회하려 시도하지 않는다 — 이 프롬프트는 사람이 직접 답하도록 설계된 것이다. 에이전트 실행 환경이
   비대화형이라 이 훅에서 막히는 것은 **의도된 동작**이며, 실패로 취급하지 말고 사람에게 "터미널에서 직접
   커밋해달라"고 안내한다.

## 3. 그 외 작업 원칙

- 대규모 리팩터링, 스키마 변경(Prisma migration 포함), 의존성 주요 버전 업그레이드는 먼저 계획을 설명하고
  사람의 동의를 받은 뒤 진행한다.
- 여러 패키지(`packages/web`, `packages/server`, `packages/bot`)에 걸친 변경은 각 패키지별로 어떤 영향이
  있는지 설명한다.
- 테스트/빌드가 로컬에서 실패하는 상태로 작업을 끝내지 않는다. 끝내야 한다면 실패 상태와 원인을 명확히 남긴다.

## 4. 이 문서와 CONTRIBUTING.md의 관계

- `CONTRIBUTING.md`: 사람 기여자를 포함해 이 프로젝트의 공통 규칙(커밋 컨벤션, 훅 사용법)
- `AGENTS.md`(이 문서): 그중에서도 **AI 에이전트에게 특별히 적용되는 추가 제약**
- 두 문서가 충돌하면 이 문서(`AGENTS.md`)가 에이전트 행동에 우선한다.
